/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MoviesListImageDirective } from './movies-list-image.directive';

describe('Directive: MoviesListImage', () => {
  it('should create an instance', () => {
    let directive = new MoviesListImageDirective();
    expect(directive).toBeTruthy();
  });
});
